// if (document.location.protocol !== "https:") {
//     document.location = document.URL.replace(/^http:/i, "https:");
// }

function byId(id) {
    return document.getElementById(id);
}

function qSel(qSel) {
    return document.querySelector(qSel);
}
